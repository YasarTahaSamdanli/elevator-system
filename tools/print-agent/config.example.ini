[agent]
; Backend API taban adresi (sonuna /api/v1 dahil).
api_url = http://localhost:8000/api/v1

; "Yazici Ajani" kullanicisinin Sanctum token'i (README'ye bakin).
token =

; Bos birakilirsa Windows varsayilan yazicisi kullanilir.
printer =

; Kuyruk yoklama araligi (saniye).
poll_seconds = 30
